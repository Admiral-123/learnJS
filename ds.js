"use strict" // treat all js code as newer version of js\


// alert(4+8) // it works in browser not in node


let x = "hlo"
let len = 3
let isStr = true


/* 
number range= 2^53
bigint - for very large no.
boolean => true/false
null -> in js it is standalone value
undefined -> var declared but not assigned
symbol -> for unique


object

*/

console.table([typeof isStr, typeof x, typeof len])

console.log(typeof null)        // object
console.log(typeof undefined)   // undefined